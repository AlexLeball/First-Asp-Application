@using project_5.Models.Entities
@model List<Car>



@{
    ViewData["Title"] = "Nos Voitures";
}

<div class="centered-container">

    <h1 class="page-title">Nos Voitures</h1>

    @if (User.IsInRole("Admin"))
    {
        <a class="btn btn-custom-back" href="@Url.Action("AddCarForm", "CarList")"> + Ajouter une Voiture</a>
    }

    <div class="car-grid">
        @foreach (var car in Model)
        {
            <div class="car-item">
                <a href="@Url.Action("CarDetails", "CarList", new { id = car.Id })">
                    <div class="car-content">
                        <img src="@car.UrlPhoto" alt="Car Image" />
                        <div class="car-info">
                           <h5>Prix de vente @car.SalePrice</h5>
                            <p>Année @car.Year</p>
                            <p>Marque @car.Brand.Name </p>
                            <p>Modèle @car.CarModel.Model</p>
                           <p>Finition: @car.Finition</p>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
</div>